### Preparation

On linux (or WSL):

```
sudo apt install make
sudo apt install gcc

sudo apt install python3-pip
pip install matplotlib
pip install numpy
```

### Build
```
make
```

### Run

```
./run.sh
```